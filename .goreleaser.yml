before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - id: "neo_client"
    main: "./cmd/client"
    binary: "./neo_client/neo"
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64

  - id: "dummy"
    main: "./cmd/client"
    binary: "neo"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
    hooks:
      post:
        - "mv neo_client_docker neo_{{ .Version }}"
        - "zip -r neo_{{ .Version }}.zip neo_{{ .Version }}"
        - "rm -rf neo_{{ .Version }}"
        - "mv neo_{{ .Version }}.zip dist/"
        - "ls -lh dist/neo_{{ .Version }}.zip"

  - id: "neo_server"
    main: "./cmd/server"
    binary: "./neo_server/neo_server"
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64

archives:
  - id: full_binaries
    format: zip
    builds:
      - "neo_client"
      - "neo_server"
    name_template: "{{ .ProjectName }}_full_{{ .Version }}_{{ .Os }}"
    replacements:
      darwin: macOS
      386: i386
      amd64: x64
    format_overrides:
      - goos: linux
        format: tar.gz
    files:
      - README.md
      - neo_server/config.yml
      - neo_client/config.yml

  - id: server
    format: zip
    builds:
      - "neo_server"
    name_template: "{{ .ProjectName }}_server_{{ .Version }}_{{ .Os }}"
    replacements:
      darwin: macOS
      386: i386
      amd64: x64
    format_overrides:
      - goos: linux
        format: tar.gz
    files:
      - neo_server/*

release:
  extra_files:
    - glob: "dist/neo_client_{{ .Version }}.zip"

checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
